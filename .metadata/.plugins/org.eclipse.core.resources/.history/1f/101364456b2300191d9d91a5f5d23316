#include <bits/stdc++.h>

using namespace std;

int main() {
	int N, P, Q, R;
	cin >> N >> P >> Q >> R;

	vector<int> A (N, 0);
	vector<vector<int>> DP (3, vector<int> (N, 0));

	for(int i = 0; i < N; i++){
		cin >> A[i];
	}

	for(int i = 0; i < N; i++){
		for(int j = 0; j < N; j++){

		}
	}

	return 0;
}
