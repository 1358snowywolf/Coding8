#include <bits/stdc++.h>

using namespace std;
vector<vector<vector<int>>> DP (100, vector<vector<int>> (101, vector<int> (100, 2000000000)));

int main() {
	int N;
	cin >> N;

	vector<int> log (N, 0);

	for(int i = 0; i < N; i++){
		cin >> log[i];
	}

	return 0;
}
