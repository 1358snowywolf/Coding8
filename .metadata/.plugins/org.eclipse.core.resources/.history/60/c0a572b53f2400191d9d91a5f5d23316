#include <bits/stdc++.h>

using namespace std;
vector<vector<vector<int>>> DP;

int main() {
	int N;
	cin >> N;

	vector<int> log (N, 0);

	for(int i = 0; i < N; i++){
		cin >> log[i];
	}

	return 0;
}
